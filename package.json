{
  "name": "sgtra",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gestión de Taller de Refrigeración Automotriz",
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "db:init": "npm --prefix backend run db:init",
    "setup": "npm run install:all && npm run db:init",
    "dev": "node scripts/run-development.js",
    "start": "npm run dev",
    "check": "npm --prefix backend run check && npm --prefix backend test && npm --prefix frontend run lint && npm --prefix frontend test && npm --prefix frontend run build",
    "smoke": "npm --prefix backend run smoke"
  },
  "engines": {
    "node": ">=20.19"
  }
}
